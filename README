@Author: J.A. Simmons V
@Date: 25 Jan 2013
This script will grab the twice daily backups from /var/atlassian/application-data/jira/export, tar the existing files, move the tar file to a directory for off site backup and then move the just backed up files to a holding folder for deletion.
